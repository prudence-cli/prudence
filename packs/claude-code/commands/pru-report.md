---
description: Write the ledger receipt to HTML
---

# /pru:report

Render the books as a single-file receipt. Thin glue: the daemon owns
the truth, this pane only reads.

!`pru report`

Report back the file path plus the two headline figures (posted spend,
set aside) quoted exactly. The HTML opens from `file://` with no
server — send or archive it freely.
